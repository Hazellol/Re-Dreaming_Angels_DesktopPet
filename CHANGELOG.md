# 更新日志

## [1.1.2] - 2026-09-26

本版本修复若干已知问题，并合并社区贡献者的 5 个 Pull Request。

### 修复

- **角色周围出现黑色矩形块**（部分显卡 / 驱动 / 系统设置下出现）
  - 窗口形状裁剪后强制重绘并显式设置透明背景；新增「设置 → 显示兼容 → 窗口区域裁剪」开关，必要时可关闭（关闭后可能重现视频黑屏，二者取其一）
- **角色待机走动速度异常**：重力模式下角色被甩飞后可能瞬间横穿屏幕（走动时长改为按实际位移计算，速度恒定）
- **音乐播放器**
  - 列表处于展开状态时导入歌曲不显示（需收起再展开）
  - 播放进度条无法拖动、总时长始终显示 `0:00`
  - 用户导入的曲目不显示删除按钮
- 合并社区贡献者的 5 个 Pull Request：
  - 面板可见性判定改用计算样式（修复窗口长期处于可交互态、鼠标穿透失效、拖动守护误触发）
  - 穿透状态统一经 `setMouseIgnore()` 设置（修复菜单关闭后点击失效、需重启才能恢复）
  - 台词气泡随角色缩放（字号 / 内边距 / 描边 / 尾巴按比例缩放）
  - 右键菜单「锁定」改为状态式文案（原文案与实际动作语义相反）
  - 支持在 macOS 上运行（Win32 专属逻辑按平台隔离 + mac 打包配置 + 启动器 + 说明文档）

### 新增

- 音乐播放器支持删除用户导入的歌曲

### 变更

- 待机台词气泡放大 1.3 倍、停留时间延长 2 秒，并改为打字机逐字显示
- 音乐播放器控制按钮重绘：播放 / 暂停 / 上一首 / 下一首 / 添加改用图标；循环模式按钮改为胶囊式并显示「顺序 / 单曲」，单曲循环时整块高亮

## [1.1.1] - 2026-09-20

本版本在 1.1.0 的 TTS 功能基础上修复已知问题并优化使用体验。

### 新增

- TTS 下载过程**实时写入后台日志**：百分比、已下载 / 总大小、实时速度、剩余时间（每 2 秒一条，多分卷分别记录）
- 解压阶段记录耗时，下载完成记录平均速度
- **支持自定义目录**：推理环境目录、服务脚本、语音包目录均可通过「浏览」按钮指定
  - 选择推理环境目录时自动递归探测 `python.exe` 与 `api_v2.py`
  - 选择语音包目录时校验是否包含 `<角色>/emotions.json`
- 控制台新增「数据目录」与「日志文件」按钮，可直接定位运行时数据

### 变更

- 语音设置页重新排版：按「运行环境 / 下载与安装 / 角色语音包 / 朗读设置 / 高级」分区，统一标签宽度与行高，长文本不再溢出
- 控制台窗口支持自由缩放（最小 640 × 520），内容自适应
- 服务状态栏改为独立状态卡，置于语音页顶部

### 修复

- 打包版 TTS 数据目录判定不确定（原依赖进程工作目录，便携版双击时不可控）→ 改为固定规则：打包版使用 `%APPDATA%\<应用名>\tts`，源码版使用 `<项目>\tts`
- 语音包目录为空时被优先匹配，导致读取不到 `emotions.json`
- 配置文件带 UTF-8 BOM 时解析失败
- 服务首次启动等待时间过短（20 秒）导致误报「服务不可用」→ 延长至 180 秒
- 服务忙时探活超时导致状态在「运行中 / 未运行」之间反复跳变 → 增加抖动抑制（连续 3 次失败才判定停止）
- 代理或自签根证书环境下网络请求失败 → 改用 Electron `net` 模块（信任系统证书库），并同步修复本机 `git` 配置
- 已安装推理环境时重复下载（现自动跳过，避免重复拉取约 5.45 GB）
- 合成前未过滤台词中的舞台指示（`（）`、`[]` 等被一并朗读）

## [1.1.0] - 2026-09-19

本次更新新增本地语音合成（TTS）与多显示器支持，并修复了若干交互与性能问题。

### 新增

- **本地语音合成（TTS）**
  - 基于 GPT-SoVITS v2ProPlus 的本地推理，将 AI 回复文本合成为语音播放
  - 情绪映射：解析 AI 回复中的 `<mood:...>` 标签，按情绪选择参考音频（支持 neutral / happy / sad / angry / shy / think / tired / proud）
  - 情绪映射由 `tools/build_emotions.mjs` 从训练标注文件自动生成，每档情绪包含多条候选参考
  - 可选功能，默认关闭；模型与运行时按需下载，不随整合包分发
  - 控制台新增「语音（TTS）」设置页：启用开关、推理设备（GPU / CPU）、下载源选择、一键下载推理环境、按角色下载语音包、朗读范围、空闲卸载、后台日志查看
  - 一键下载：自动分卷下载、解压、探测运行时、写入配置，并自动将推理配置切换为 v2ProPlus
  - 已安装时自动跳过下载，避免重复拉取约 5.45 GB 资源
  - 服务管理：自动探活（含状态抖动抑制）、按需启动、空闲自动卸载、失败静默降级
  - 合成音频缓存，支持按角色分目录保存（文件名含情绪与台词片段）
  - 自动过滤台词中的舞台指示（`（）()【】[]` 等），仅朗读正文
  - 自动随请求携带角色模型权重（`gpt_path` / `sovits_path`），确保音色正确
  - 应用日志：`tts/logs/app.log`（可通过控制台查看或打开所在目录）

- **多显示器支持**
  - 控制台新增「多显示器」设置：主显示器 / 副显示器 / 跟随角色
  - 窗口切换显示器时自动补偿角色坐标，视觉位置保持不变
  - 跟随模式下拖动角色跨越显示器时，窗口自动跟随（含切换防抖）

- **其他**
  - 控制台新增「版本与更新」：查询仓库最新 Release 与最近提交，可跳转发布页
  - 控制台窗口支持自由缩放（最小 640 × 520），内容自适应布局
  - 设置项未保存时，切换页面或关闭控制台会提示是否保存
  - 服务状态与启停按钮联动（运行中 / 启动中 / 未运行）

### 修复

- 角色动画周期性重置（改用显式动作状态标记，替代按动画名匹配）
- 视频播放时窗口黑屏（通过设置窗口区域，使视频区域不被非透明窗口覆盖）
- 内置播放器无法播放部分视频（K1/K2 问题）
- 菜单与气泡被窗口边缘裁剪（增大窗口区域留白；补充遗漏的命中区域）
- 拖动状态卡死导致点击失效（增加多重看门狗与物理按键交叉校验）
- 后台自愈时重新加载页面导致的界面重置
- 空闲呼吸动作周期性卡顿
- 全屏窗口不渲染、隐藏全部角色后仍占用渲染资源（新增按需暂停渲染）
- 多显示器混合 DPI 环境下窗口尺寸异常（自动回退主屏工作区）
- TTS 配置文件带 BOM 时解析失败
- TTS 首次启动等待时间过短导致误报服务不可用（延长至 180 秒）
- 语音包目录为空时错误地优先匹配该目录，导致读取不到情绪映射
- 代理 / 自签证书环境下网络请求失败（改用 Electron net 模块，使用系统证书库）

### 优化

- 渲染性能：空闲帧率节流、交互时解除限制、可配置帧率上限
- 内存占用：隐藏角色的纹理资源延迟释放、控制台窗口懒加载、音频流式读取
- 启动参数：禁用多项未使用的 Chromium 特性以降低资源占用
- 鼠标穿透判定：主进程轮询与渲染进程命中区域上报相结合，减少无效切换

## [1.0.0] - 2026-09-07

首个公开发布版本。

### 新增

- 三名角色（爱芮 / 千夏 / 南宫羽）源码级 Spine 渲染：动作、全套表情、走动 / 搬起 / 飞行 / 弹跳姿态
- 桌面互动：单击互动（随机台词、音效、表情，30% 触发 AI 即时反应）、双击聊天、拖动与甩飞（重力落地、边界反弹）、右键菜单
- 基于 DeepSeek API 的角色扮演对话：手机面板与头顶气泡两种样式、情绪动作联动、时间感知、历史记录（复制 / 编辑重发）
- 联网模式：DeepSeek Responses API `web_search` 与独立联网版人设
- 角色互聊：靠近触发、两人 / 三人对话、话题轮换、记忆注入，参数可调
- 音频系统：互动音效、BGM 播放器（支持导入）、三路音量独立调节
- 控制面板：音量、播放器、角色大小、待机与移动频率、AI 对话配置、互聊设置
- 系统集成：窗口置顶、鼠标穿透、单实例锁、开机自启动、托盘图标、救援快捷键
- 打包支持：便携版（单文件）与 NSIS 安装版
